import { UploadFileRequest } from './upload-file-request';

describe('UploadFileRequest', () => {
  it('should create an instance', () => {
    expect(new UploadFileRequest()).toBeTruthy();
  });
});
