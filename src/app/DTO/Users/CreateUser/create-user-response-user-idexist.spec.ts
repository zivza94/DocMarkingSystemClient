import { CreateUserResponseUserIDExist } from './create-user-response-user-idexist';

describe('CreateUserResponseUserIDExist', () => {
  it('should create an instance', () => {
    expect(new CreateUserResponseUserIDExist()).toBeTruthy();
  });
});
