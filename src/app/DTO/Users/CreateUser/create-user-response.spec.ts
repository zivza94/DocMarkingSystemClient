import { CreateUserResponse } from './create-user-response';

describe('CreateUserResponse', () => {
  it('should create an instance', () => {
    expect(new CreateUserResponse()).toBeTruthy();
  });
});
