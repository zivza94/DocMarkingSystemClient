import { CreateUserResponseOK } from './create-user-response-ok';

describe('CreateUserResponseOK', () => {
  it('should create an instance', () => {
    expect(new CreateUserResponseOK()).toBeTruthy();
  });
});
