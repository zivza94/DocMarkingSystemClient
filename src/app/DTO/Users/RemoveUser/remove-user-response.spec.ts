import { RemoveUserResponse } from './remove-user-response';

describe('RemoveUserResponse', () => {
  it('should create an instance', () => {
    expect(new RemoveUserResponse()).toBeTruthy();
  });
});
