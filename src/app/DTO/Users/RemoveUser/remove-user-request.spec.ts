import { RemoveUserRequest } from './remove-user-request';

describe('RemoveUserRequest', () => {
  it('should create an instance', () => {
    expect(new RemoveUserRequest()).toBeTruthy();
  });
});
