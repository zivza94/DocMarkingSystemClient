import { LoginResponseInvalidUserID } from './login-response-invalid-user-id';

describe('LoginResponseInvalidUserID', () => {
  it('should create an instance', () => {
    expect(new LoginResponseInvalidUserID()).toBeTruthy();
  });
});
