import { GetSharedUsersRequest } from './get-shared-users-request';

describe('GetSharedUsersRequest', () => {
  it('should create an instance', () => {
    expect(new GetSharedUsersRequest()).toBeTruthy();
  });
});
