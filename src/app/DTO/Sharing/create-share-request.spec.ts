import { CreateShareRequest } from './create-share-request';

describe('CreateShareRequest', () => {
  it('should create an instance', () => {
    expect(new CreateShareRequest()).toBeTruthy();
  });
});
