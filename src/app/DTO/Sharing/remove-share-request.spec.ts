import { RemoveShareRequest } from './remove-share-request';

describe('RemoveShareRequest', () => {
  it('should create an instance', () => {
    expect(new RemoveShareRequest()).toBeTruthy();
  });
});
