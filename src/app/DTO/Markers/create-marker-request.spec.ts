import { CreateMarkerRequest } from './create-marker-request';

describe('CreateMarkerRequest', () => {
  it('should create an instance', () => {
    expect(new CreateMarkerRequest()).toBeTruthy();
  });
});
