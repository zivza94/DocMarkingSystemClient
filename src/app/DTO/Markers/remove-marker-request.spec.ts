import { RemoveMarkerRequest } from './remove-marker-request';

describe('RemoveMarkerRequest', () => {
  it('should create an instance', () => {
    expect(new RemoveMarkerRequest()).toBeTruthy();
  });
});
