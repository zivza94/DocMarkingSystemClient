import { EndLiveDrawRequest } from './end-live-draw-request';

describe('EndLiveDrawRequest', () => {
  it('should create an instance', () => {
    expect(new EndLiveDrawRequest()).toBeTruthy();
  });
});
