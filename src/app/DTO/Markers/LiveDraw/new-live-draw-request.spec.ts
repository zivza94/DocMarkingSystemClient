import { NewLiveDrawRequest } from './new-live-draw-request';

describe('NewLiveDrawRequest', () => {
  it('should create an instance', () => {
    expect(new NewLiveDrawRequest()).toBeTruthy();
  });
});
