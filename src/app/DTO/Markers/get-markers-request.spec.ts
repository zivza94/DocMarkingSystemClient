import { GetMarkersRequest } from './get-markers-request';

describe('GetMarkersRequest', () => {
  it('should create an instance', () => {
    expect(new GetMarkersRequest()).toBeTruthy();
  });
});
