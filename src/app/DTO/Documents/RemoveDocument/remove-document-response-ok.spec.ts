import { RemoveDocumentResponseOK } from './remove-document-response-ok';

describe('RemoveDocumentResponseOK', () => {
  it('should create an instance', () => {
    expect(new RemoveDocumentResponseOK()).toBeTruthy();
  });
});
