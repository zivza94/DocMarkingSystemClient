import { GetDocumentResponseOK } from './get-document-response-ok';

describe('GetDocumentResponseOK', () => {
  it('should create an instance', () => {
    expect(new GetDocumentResponseOK()).toBeTruthy();
  });
});
